{
  "installCommand": "cd frontend && rm -f package-lock.json && npm install",
  "buildCommand": "cd frontend && npm install lightningcss-linux-x64-gnu --no-save && npm run build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "functions": {
    "api/**/*.py": {
      "excludeFiles": "{backend/tests/**,frontend/**,docs/**,.venv/**,.pytest_cache/**}"
    }
  }
}
